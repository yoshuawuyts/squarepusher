'use strict';

/**
 * Module dependencies
 */

var React = require('react');

/**
 * Define view
 */

module.exports = React.createClass({
  render: function () {
    return React.DOM.div(null, 'hello world')
  }
});